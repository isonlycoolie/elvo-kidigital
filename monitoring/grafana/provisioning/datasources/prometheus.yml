# This file is intentionally left as a pointer to avoid VS Code schema false positives.
# Active Grafana datasource provisioning is defined in:
# monitoring/grafana/provisioning/datasources/elvo-prometheus-datasource.yml
